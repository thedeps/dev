<%= form_for(@championship, :html => { :class => "form-horizontal", :role => "form" }) do |f| %>
  <% if @championship.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@championship.errors.count, "error") %> prohibited this championship from being saved:</h4>

      <ul>
      <% @championship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, 'Nome'%>
    <%= f.text_field :name, :class => "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :prize, 'Prêmio' %>
    <%= f.text_field :prize, :class => "form-control" %>
  </div>
  <div class="form-group">
    <%= f.submit :class => "btn btn-primary" %>
    <%= link_to championships_path, :class => 'btn btn-default' do %>
      Voltar
    <% end %>
  </div>
<% end %>
